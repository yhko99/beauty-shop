<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.dao.MemberDAO">

    <!-- 회원가입 -->
    <insert id="insert" parameterType="memberVO">
        INSERT INTO t_member (member_id, member_pw, member_name, member_email)
        VALUES (#{memberId}, #{memberPw}, #{memberName}, #{memberEmail})
    </insert>

    <!-- 로그인 -->
    <select id="selectOne" parameterType="memberVO" resultType="memberVO">
        SELECT member_id    as memberId
             , member_pw    as memberPw
             , member_name  as memberName
             , member_email as memberEmail
          FROM t_member
         WHERE member_id = #{memberId}
           AND member_pw = #{memberPw}
    </select>

    <!-- 아이디 중복 체크 -->
    <select id="selectCount" parameterType="String" resultType="int">
        SELECT COUNT(*)
          FROM t_member
         WHERE member_id = #{memberId}
    </select>

    <!-- 얼굴 데이터 저장 -->
    <update id="updateFaceDescriptor" parameterType="memberVO">
        UPDATE t_member
           SET face_descriptor = #{faceDescriptor}
         WHERE member_id = #{memberId}
    </update>

    <!-- 얼굴로 회원 찾기 -->
    <select id="selectByFace" parameterType="String" resultType="memberVO">
        SELECT member_id    as memberId
             , member_name  as memberName
             , member_email as memberEmail
             , face_descriptor as faceDescriptor
          FROM t_member
         WHERE member_id = #{memberId}
    </select>

    <!-- 전체 회원 얼굴 데이터 조회 -->
    <select id="selectAllFace" resultType="memberVO">
        SELECT member_id    as memberId
             , member_name  as memberName
             , face_descriptor as faceDescriptor
          FROM t_member
         WHERE face_descriptor IS NOT NULL
    </select>

</mapper>